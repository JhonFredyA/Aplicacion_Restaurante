const { Router } = require('express')

const router = Router()

router.post('/recordOrder', async (req, res) => {
    try {
        
    } catch (error) {
        console.error(error);
    }
})

module.exports = router